# Docker Apache PHP8 Wrapper
Wrapper Docker image for Apache based PHP 8 projects

## Start (`start.bat`)
```sh
docker compose up -d --build
```

## Stop (`stop.bat`)
```sh
docker compose down
```